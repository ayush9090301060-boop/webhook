function login(username, pas) {
  // Hardcoded credentials for testing
  const adminUser = "admin";
  const adminPass = "supersec";
  
  if (username === adminUser && password === adminPass) {
    return true;
  }
}
